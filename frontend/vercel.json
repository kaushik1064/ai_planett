{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "env": {
    "VITE_BACKEND_BASE_URL": "https://YOUR_RENDER_BACKEND.onrender.com"
  },
  "build": {
    "env": {
      "VITE_BACKEND_BASE_URL": "https://YOUR_RENDER_BACKEND.onrender.com"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

